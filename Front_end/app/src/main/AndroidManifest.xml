<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.rescue_pets">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:usesCleartextTraffic="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Rescue_Pets"
        tools:targetApi="31">

        <!-- MainActivity is now launcher -->
        <activity
            android:name=".Volunteer.Volunteer_Registration"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Splash navigates here -->
        <activity android:name=".ChooseActiity" android:exported="false" />

        <!-- Other Activities -->
        <activity android:name=".Volunteer.Forgot_Password_Volunteer" android:exported="false" />
        <activity android:name=".Admin.Forgot_Password_Admin" android:exported="false" />
        <activity android:name=".Volunteer.Volunteer_Dashboard" android:exported="false" />
        <activity android:name=".User.User_Registration_Activity" android:exported="false" />
        <activity android:name=".Volunteer.Login_Volunteer" android:exported="false" />
        <activity android:name=".Admin.DashBoard_Admin" android:exported="false" />
        <activity android:name=".Admin.Admin_Login" android:exported="false" />
        <activity android:name=".User.Dash_Board_User" android:exported="false" />
        <activity android:name=".User.Forgot_Password_User" android:exported="false" />
        <activity android:name=".User.User_Login_Activity" android:exported="false" />
        <activity android:name=".MainActivity" android:exported="false" />

    </application>

</manifest>
